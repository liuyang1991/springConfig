eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true
    instance-id: ${eureka.instance.hostname}:${server.port}
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

eurekaServiceUrl: http://localhost:8761/eureka/
myKey: keyValuedfdsfsdf
newKey: newKey8798098